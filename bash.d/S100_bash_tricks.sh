#When changing directory small typos can be ignored by Bash
shopt -s cdspell

#Ctrl+D conveniently exits Bash, but sometimes too conveniently
#Specify that it must be pressed twice to exit:
export IGNOREEOF=1
